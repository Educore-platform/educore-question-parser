NODE_ENV=development
PORT=3000

# ── PostgreSQL ─────────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=
DB_NAME=educore

# Optional: set to "false" when migrations already ran (Docker entrypoint). Omit locally — defaults to true (Nest runs migrations in production).

# ── Redis ──────────────────────────────────────────────
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
# Required when Redis uses AUTH (requirepass or managed Redis). Omit for local open Redis.
REDIS_PASSWORD=
